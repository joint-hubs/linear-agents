# planning/ — runtime I/O (treść gitignored)

- **inbox/** — wejście agenta PLAN: notatki głosowe (transkrypt) + artefakty (`.md/.txt/.docx`, screeny).
- **briefs/** — briefy generowane przez PLAN (discovery synthesis, ≤1 strona).

Treść jest gitignored (`.gitignore`), katalogi trzymane przez `.gitkeep`.
