{
  "_doc": "Mapa: Linear project -> workspace + repo + deploy target. Workspace: joi (Neo/AU/Fenix) or pisi (PISI). Deploy: gcp-vm via SSH. GPU platform (lambda-gpu) deferred — see platforms.lambda-gpu.",
  "default": {
    "deploy": { "type": "gcp-vm", "vm": "${GCP_VM_NAME}", "ssh": "${GCP_SSH_USER}", "project": "${GCP_PROJECT}", "zone": "${GCP_ZONE}" }
  },
  "platforms": {
    "lambda-gpu": {
      "status": "deferred",
      "note": "GPU VM platform for Ollama deploys via SSH, not a project. Separate from gcp-vm. Deferred until needed."
    }
  },
  "projects": [
    {
      "linearProject": "Neo",
      "workspace": "joi",
      "repo": "${GITHUB_ROOT}/joint-flows",
      "deploy": { "type": "gcp-vm", "vm": "TODO-vm-name", "ssh": "TODO" }
    },
    {
      "linearProject": "AU",
      "workspace": "joi",
      "repo": "${GITHUB_ROOT}/office",
      "deploy": { "type": "gcp-vm", "vm": "TODO-vm-name", "ssh": "TODO" }
    },
    {
      "linearProject": "FENIX",
      "workspace": "joi",
      "repo": "${GITHUB_ROOT}/linear-agents",
      "deploy": { "type": "none", "note": "pilot test project — no deploy target" },
      "note": "Linear project used by team FOC (e.g. FOC-104) — tracks this repo's own tooling work."
    },
    {
      "linearProject": "Fenix v2",
      "workspace": "joi",
      "repo": "${GITHUB_ROOT}/linear-agents",
      "deploy": { "type": "none", "note": "pilot test project — no deploy target" },
      "note": "Linear project used by team JOI (e.g. JOI-259) — same as FENIX above, tracks this repo."
    },
    {
      "linearProject": "PISI",
      "workspace": "pisi",
      "repo": "${GITHUB_ROOT}/gantt-pisi",
      "deploy": { "type": "gcp-vm", "vm": "TODO-vm-name", "ssh": "TODO" }
    },
    {
      "linearProject": "linear-agents",
      "workspace": "joi",
      "teamKey": "${LINEAR_TEAM_KEY_FEN}",
      "projectId": "${LINEAR_PROJECT_ID_FEN}",
      "repo": "${GITHUB_ROOT}/linear-agents",
      "deploy": { "type": "none", "note": "pilot test project — no deploy target" },
      "note": "Pilot test project for linear-agents orchestration. Team id configured via LINEAR_TEAM_UUID_FEN. See docs/BUILD-BACKLOG Faza C / docs/STATE.md."
    }
  ]
}
